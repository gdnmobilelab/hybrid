{
    "debug": [
              {
              "scope": "${INITIAL_URL}",
              "src": "${INITIAL_URL}sw.js"
              }
    ],
    "release": [
        {
        "scope": "${INITIAL_URL}",
        "src": "${INITIAL_URL}sw.js"
        }
    ]
}
